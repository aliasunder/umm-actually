diff --git a/src/greeter.ts b/src/greeter.ts
index 1111111..2222222 100644
--- a/src/greeter.ts
+++ b/src/greeter.ts
@@ -1,5 +1,6 @@
 export const greet = (name: string): string => {
-  return "Hello " + name
+  const trimmedName = name.trim()
+  return `Hello ${trimmedName}`
 }

 export const farewell = (name: string): string => {
@@ -140,5 +141,7 @@ export const shout = (message: string): string => {
 const registry = new Map<string, string>()

 export const register = (key: string, value: string): void => {
+  if (key === "") throw new Error("key must not be empty")
+
   registry.set(key, value)
 }
diff --git a/src/added-file.ts b/src/added-file.ts
new file mode 100644
index 0000000..3333333
--- /dev/null
+++ b/src/added-file.ts
@@ -0,0 +1,3 @@
+export const brandNew = (): number => {
+  return 42
+}
diff --git a/src/removed-file.ts b/src/removed-file.ts
deleted file mode 100644
index 4444444..0000000
--- a/src/removed-file.ts
+++ /dev/null
@@ -1,3 +0,0 @@
-export const obsolete = (): number => {
-  return 0
-}
diff --git a/src/old-name.ts b/src/new-name.ts
similarity index 90%
rename from src/old-name.ts
rename to src/new-name.ts
index 5555555..6666666 100644
--- a/src/old-name.ts
+++ b/src/new-name.ts
@@ -10,4 +10,4 @@ export const keep = (): string => {

 export const renamedThing = (): string => {
-  return "old"
+  return "new"
 }
diff --git a/assets/logo.png b/assets/logo.png
index 7777777..8888888 100644
Binary files a/assets/logo.png and b/assets/logo.png differ
